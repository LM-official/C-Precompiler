// non sense header file

/*the #define directive lets you define a macro, i.e. a symbol.
 * the syntax is #define MACRO_NAME MACRO_VALUE
 * The preprocessor reads the macro definition and when in the code
 * it encounters the symbol (MACRO_NAME) it replaces it with the definition.
 *
 * THE MACRO NAME MUST BE A VALID IDENTIFIER
 *
 */

#define CONST1 37
#define PI 3.14

typedef float myFloat-t; //myFloat-t is an identifier that names a new data type

